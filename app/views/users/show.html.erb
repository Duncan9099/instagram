<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% if @user.profile.attached? %>
      <img src="<%= (url_for(@user.profile))%>">
    <% end %>
    <br>
    <%= @posts.each do |post| %>
      <% if post.post_picture.attached? %>
        <img src="<%= (url_for(post.post_picture)) %>">
      <% end %>
    <% end %>
  </body>
</html>
