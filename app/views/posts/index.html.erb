<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Skinstagram PostFeed</title>

    <style>
      img {
        display: block;
        margin-left: auto;
        margin-right: auto;
        width: 100%;
        overflow:hidden
      }

      .post_container {
        height:400px;
        overflow:hidden
      }

    </style>

  </head>
  <body>
    <h2 style="text-align:center">All Posts</h2>
    <div class="container">
      <div class="row">
        <div class="col-3"></div>
        <div class="col-6">
            <% @posts.each do |post| %>
              <% if post.post_picture.attached? %>
                <div style="height:500px; overflow:hidden">
                  <img src="<%= (url_for(post.post_picture))%>">
                </div>
                <h5><a href="/users/<%= post.user.id %>"><%= post.user.username %></a></h5>
                <p><%= post.content %></p>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="col-3"></div>
      </div>
    </div>
  </body>
</html>
